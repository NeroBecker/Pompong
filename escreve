function setup() {
  createCanvas(400, 400);
 background("black")}

function draw() {
 stroke("red")
  fill("red")
   //console.log(mouseIsPressed)
   if(mouseIsPressed){
     rect(mouseX,mouseY,5,5)
   }
}
